#!/usr/bin/env node
var debug = require('debug')('Jupiter-server');
var app = require('../app');

app.set('port', process.env.PORT || 80);

var server = app.listen(app.get('port'), function() {
  debug('Jupiter server listening on port ' + server.address().port);
});